<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="TicTacToeWinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TicTacToeWinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d"
    Title="TicTacToeWinUI">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="TicTacToeGrid" 
          Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" 
          RowSpacing="4"
          ColumnSpacing="4"
          Padding="4">

        <!-- Define 3 equally sized rows spaced out-->
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Define 3 equally sized columns spaced out-->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        

        <!-- Example of placing Buttons in the grid cells -->
        <!-- Row 0 -->
        <Button Content="" Tag="0,0" Grid.Row="0" Grid.Column="0" Click="ChangeSymbol" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontSize="72"/>
        <Button Content="" Tag="0,1" Grid.Row="0" Grid.Column="1" Click="ChangeSymbol" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontSize="72"/>
        <Button Content="" Tag="0,2" Grid.Row="0" Grid.Column="2" Click="ChangeSymbol" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontSize="72"/>

        <!-- Row 1 -->
        <Button Content="" Tag="1,0" Grid.Row="1" Grid.Column="0" Click="ChangeSymbol" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontSize="72"/>
        <Button Content="" Tag="1,1" Grid.Row="1" Grid.Column="1" Click="ChangeSymbol" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontSize="72"/>
        <Button Content="" Tag="1,2" Grid.Row="1" Grid.Column="2" Click="ChangeSymbol" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontSize="72"/>

        <!-- Row 2 -->
        <Button Content="" Tag="2,0" Grid.Row="2" Grid.Column="0" Click="ChangeSymbol" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontSize="72"/>
        <Button Content="" Tag="2,1" Grid.Row="2" Grid.Column="1" Click="ChangeSymbol" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontSize="72"/>
        <Button Content="" Tag="2,2" Grid.Row="2" Grid.Column="2" Click="ChangeSymbol" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontSize="72"/>

        <!-- Restart Game Menu -->
        <ContentDialog
        x:Name="RestartDialog"
        Title="Game Over"
        PrimaryButtonText="Yes"
        SecondaryButtonText="No"
        DefaultButton="Primary">

            <StackPanel Spacing="8">
                <TextBlock
            x:Name="ResultText"
            FontSize="18"
            FontWeight="SemiBold"
            TextAlignment="Center" />

                <TextBlock
            Text="Would you like to restart the game?"
            TextAlignment="Center" />
            </StackPanel>
            
        </ContentDialog>
    </Grid>
</Window>
